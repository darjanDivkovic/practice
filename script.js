// Coding challenge part 9

let person = {

    name: 'Dacija',
    age: 21,
    gender: 'male'
};

if( person.age <= 17 ){
    alert(`${person.name} is an MINOR`);
}
else alert(`${person.name} is an ADULT`);


